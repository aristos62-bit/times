# Φάση 5 — Βήμα 2: Data aggregations (26-09-2026)

> Κατάσταση: **Κλειστό.**
> Read-only `SUM(lineTotalCents) GROUP BY` ×4 στο `ReceiptDao` (όχι νέο DAO)
> + records + repo passthrough. Καμία migration, κανένα index, κανένα package.

---

## 1. Αλλαγές

- ΝΕΟ `lib/data/models/chart_totals.dart` (59 γρ.): `SupplierTotal` / `CategoryTotal` / `SubCategoryTotal` / `ItemTotal` (`{id, name, totalCents}` records — pattern `ReceiptSummary`, όχι Freezed).
- `lib/data/local/daos/receipt_dao.dart` (198→391 γρ.): `watchTotalsBySupplier/Category/SubCategory/TopItems({required from, to})` — `customSelect+readsFrom+watch` + `guardStream` (precedent συνόψεων) · `INNER JOIN` · `WHERE r.date>=? AND r.date<?` (όρια ως δίνονται, normalization στον resolver Βήματος 3) · `COALESCE(SUM,0)` · `ORDER BY totalCents DESC, name ASC` · πλήρης ordered λίστα (ΧΩΡΙΣ LIMIT — exact «Λοιπά», Q1).
- `lib/data/repositories/receipt_repository.dart` + `receipt_repository_impl.dart`: 4 passthrough + `handleError→DataLoadException` (pattern `watchRecentSummaries`). Logging μόνο στον DAO guard (tag `DB`).
- Doc-fix: NOTE `app_enums.dart:8-11` («τρίμηνο»/`StateProvider`) → περιγραφή υλοποιημένου enum.
- Fakes (6, 4 αρχεία): 4 overrides το καθένα (delegate/throw/`UnimplementedError` + import `chart_totals`).

## 2. Αποφάσεις (συμφωνημένες Q1–Q3)

- **Q1 — LIMIT:** ΟΧΙ στη SQL (με `LIMIT 10` το υπόλοιπο «Λοιπά» είναι άγνωστο). Slice στον provider Βήματος 3 (precedent `categoryTreeStreamProvider`)· το `limit` του `ChartQuery` εφαρμόζεται εκεί.
- **Q2 — JOIN:** `INNER` (ομάδες χωρίς πωλήσεις δεν γίνονται φέτες· κενό → `noPricesForPeriod`).
- **Q3 — σειρά:** `totalCents DESC, name ASC` (ντετερμινιστική).

## 3. Edge cases (καλυμμένα με tests)

Κενή περίοδος→`[]` (×2)· boundaries inclusive/exclusive· 23:59 μέσα/00:00 επομένης εκτός· απόδειξη χωρίς γραμμές και προμηθευτής/είδος χωρίς πωλήσεις εκτός (INNER)· rename ορατό (join)· RESTRICT· έκπτωση καθαρή (Δ-stat)· γραμμή 0,00€ έγκυρη· re-emit σε save· repo mapping ×4.

## 4. Tests

- Νέο `test/data/local/daos/receipt_stats_test.dart` (15) + νέο `test/data/repositories/receipt_totals_test.dart` (3 groups, 6 expects + 4 mapping).
- Σουίτα **1065/1065** ✓ (+18) · `flutter analyze` **No issues** ✓ · όλα τα αρχεία <500 γρ.

## 5. Docs

- DESIGN.md §2.1:141-143 διευκρίνιση LIMIT (doc-ορθότητα, όχι αρχιτεκτονική αλλαγή).
- Backup: `backups/2026-09-26_fase5_b2_data/` (6 lib/docs + 4 tests πριν).
