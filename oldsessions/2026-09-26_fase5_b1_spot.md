# Φάση 5 — Βήμα 1: SPoT θεμέλιο (26-09-2026)

> Κατάσταση: **Κλειστό.**
> Μόνο δηλωτικό SPoT + tests. Καμία DB, κανένας provider, κανένα widget,
> κανένα package, καμία migration. Η εφαρμογή συμπεριφέρεται byte-identical.

---

## 1. Αλλαγές

- `lib/core/constants/app_enums.dart`: `enum PeriodType { day, week, month, year, custom }` (ήταν μόνο docs, 12 γρ. → ~19 γρ.). Persist με `.name` (pattern `SettingsRepository.saveThemeMode`).
- `lib/core/constants/app_constants.dart`: `pieMaxSlices=8` · `topItemsLimit=10` (§2.1:183) · `homeChartConfigKey='home_chart_config'` (pattern `themeModeKey`) · `pieChartHeight=220.0` · `pieFallbackMaxWidth=360.0`.
- `lib/core/constants/app_strings.dart`: 11 νέα — 4 τίτλοι (`chartSupplierTitle`/`chartCategoryTitle`/`chartSubCategoryTitle`/`chartTopItemsTitle`) + 5 labels περιόδου (`periodDay/Week/Month/Year/Custom`) + `homeCustomizationTitle` + `othersSliceLabel`. Reuse `noPricesForPeriod`/`retryButton`/`currencySymbol`.
- `app_messages.dart`/`app_errors.dart`/`app_colors.dart`/`app_theme.dart`: ΚΑΜΙΑ αλλαγή (συνειδητά — βλ. §2).

## 2. Αποφάσεις επανελέγχου (συμφωνημένες)

- Κανένας νέος validator + κανένα νέο `AppErrors`: το custom range έρχεται από `showDateRangePicker` (εγγυάται `from≤to`)· corrupt persisted → defaults (pattern `_fromString→default`).
- Καμία νέα raw παλέτα: οι φέτες βγαίνουν runtime από το `ColorScheme` (dark-safe, §1:74). Τα `priceUp/priceDown` αφορούν το deferred trend.
- Fallback πίνακας χωρίς header strings (rows: όνομα + `formatCents`).
- `fl_chart` (0 imports) δεν αγγίζεται· global φίλτρο δεν δημιουργείται (ΑΠΟΣΥΡΕΤΑΙ)· `statsComingSoon`/placeholder μένουν (σβήνουν στο Βήμα 5).

## 3. Εύρημα

- Διπλή δήλωση `retryButton` μετά το edit (orphan γραμμή) → αφαίρεση, επαληθεύτηκε με ανάγνωση (μόνο 1 δήλωση).

## 4. Tests

- `app_enums_test.dart` (νέο, 3) · `app_constants_test` (+6: 5 τιμές + invariants + gate) · `app_strings_test` (+3 groups + 13 gate entries).
- Σουίτα **1047/1047** ✓ (+12) · `flutter analyze` **No issues** ✓.

## 5. Docs

- DESIGN.md: ΚΑΜΙΑ αλλαγή (προσθήκη SPoT, όχι αρχιτεκτονική — AGENTS 8).
- Backup: `backups/2026-09-26_fase5_b1_spot/` (6 αρχεία πριν).
